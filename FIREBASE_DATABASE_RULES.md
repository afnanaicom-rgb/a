# Firebase Realtime Database Rules

## المشكلة الحالية
يظهر خطأ `PERMISSION_DENIED` عند محاولة حفظ بيانات المستخدم في Firebase Database لأن القواعد الحالية لا تسمح بالكتابة.

## الحل المطلوب
يجب تحديث قواعد Firebase Realtime Database للسماح للمستخدمين المسجلين بالكتابة في بياناتهم الخاصة فقط.

## خطوات التطبيق

### 1. الذهاب إلى Firebase Console
- افتح [Firebase Console](https://console.firebase.google.com/)
- اختر مشروعك: **afnanai-640b4**

### 2. الانتقال إلى Realtime Database
- من القائمة الجانبية، اختر **Build** → **Realtime Database**
- اضغط على تبويب **Rules** (القواعد)

### 3. تحديث القواعد
استبدل القواعد الحالية بالقواعد التالية:

```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
```

### 4. نشر القواعد
- اضغط على زر **Publish** (نشر) لحفظ القواعد الجديدة

## شرح القواعد

- **`"users"`**: مسار تخزين بيانات المستخدمين
- **`"$uid"`**: متغير يمثل معرف المستخدم الفريد (UID)
- **`".read": "$uid === auth.uid"`**: يسمح للمستخدم بقراءة بياناته الخاصة فقط
- **`".write": "$uid === auth.uid"`**: يسمح للمستخدم بكتابة/تعديل بياناته الخاصة فقط

## ملاحظات مهمة

1. هذه القواعد تضمن أن كل مستخدم يمكنه الوصول فقط إلى بياناته الخاصة
2. لا يمكن لأي مستخدم قراءة أو تعديل بيانات مستخدم آخر
3. يجب أن يكون المستخدم مسجل دخول (authenticated) حتى يتمكن من الكتابة

## بعد تطبيق القواعد

بعد تحديث القواعد، سيعمل الموقع بشكل صحيح:
- المستخدمون الجدد سيتمكنون من إكمال صفحات الـ onboarding وحفظ بياناتهم
- المستخدمون القدامى سيتم توجيههم مباشرة إلى الصفحة الرئيسية
- لن يظهر خطأ `PERMISSION_DENIED` بعد الآن
